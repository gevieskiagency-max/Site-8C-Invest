import { SectionTransitionManager } from './components/SectionTransitionManager';

export default function App() {
  return (
    <main className="w-full min-h-screen bg-[#050505] text-[#f4f4f0]">
      <SectionTransitionManager />
    </main>
  );
}

